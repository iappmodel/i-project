import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.iapp.attentionwallet',
  appName: 'i Attention Wallet',
  webDir: 'dist',
}

export default config
